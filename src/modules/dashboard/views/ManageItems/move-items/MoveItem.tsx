import { Link, Outlet } from '@tanstack/react-location';
import { useQuery } from 'react-query';
import axiosInstance from '_/axios';
import './MoveItem.scss';

const MoveItem = () => {
  return <div className="MoveItem">MoveItem</div>;
};

export default MoveItem;
